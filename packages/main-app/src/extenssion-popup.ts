import React from "react";
import ReactDOM from "react-dom";
import Popup from "./popup/popup";

const rootElement = document.getElementById("root");
ReactDOM.render(React.createElement(Popup), rootElement);
