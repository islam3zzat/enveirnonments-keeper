{
  "name": "environments-keeper",
  "version": "3.0.0",
  "license": "MIT",
  "scripts": {
    "test": "cd ./packages/main-app && npm run test",
    "test:watch": "cd ./packages/main-app && npm run test",
    "build": "cd ./packages/main-app && npm run build",
    "start": "cd ./packages/main-app && npm run start"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "devDependencies": {
    "lerna": "^3.8.0"
  }
}